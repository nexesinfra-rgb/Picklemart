/// Common UI constants for spacing and layout.
///
/// This factor is used to derive edge padding from the standard card padding,
/// so that content is slightly closer to the screen borders while preserving
/// comfortable internal spacing.
const double kScreenEdgePaddingFactor = 0.6;

